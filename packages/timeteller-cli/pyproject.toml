[project]
name = "timeteller-cli"
version = "1.0.0rc2"
description = ""
readme = "README.md"
authors = [
    { name = "oyghen", email = "oyghen@duck.com" }
]
requires-python = ">=3.11"
dependencies = [
    "timeteller[plus]==1.0.0rc2",
    "typer>=0.24.1",
]

[tool.uv.sources]
timeteller = { workspace = true }

[project.scripts]
timeteller = "timeteller_cli.cli:app"

[build-system]
requires = ["uv_build>=0.11.7,<0.12.0"]
build-backend = "uv_build"
